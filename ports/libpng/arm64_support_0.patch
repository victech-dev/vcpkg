diff --git a/CMakeLists.txt b/CMakeLists.txt
index 6599e1d..64615bb 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -65,6 +65,8 @@ set(DFA_XTRA "" CACHE FILEPATH "File containing extra configuration settings")
 
 if(PNG_HARDWARE_OPTIMIZATIONS)
 
+message(STATUS DEBUG_CMAKE_SYSTEM_PROCESSOR=${CMAKE_SYSTEM_PROCESSOR})
+
 # set definitions and sources for arm
 if(CMAKE_SYSTEM_PROCESSOR MATCHES "^arm" OR
   CMAKE_SYSTEM_PROCESSOR MATCHES "^aarch64")
@@ -76,6 +78,9 @@ if(CMAKE_SYSTEM_PROCESSOR MATCHES "^arm" OR
   set_property(CACHE PNG_ARM_NEON PROPERTY STRINGS
      ${PNG_ARM_NEON_POSSIBLE_VALUES})
   list(FIND PNG_ARM_NEON_POSSIBLE_VALUES ${PNG_ARM_NEON} index)
+
+  message(STATUS DEBUG_PNG_ARM_NEON=${PNG_ARM_NEON})
+
   if(index EQUAL -1)
     message(FATAL_ERROR
             "PNG_ARM_NEON must be one of [${PNG_ARM_NEON_POSSIBLE_VALUES}]")
